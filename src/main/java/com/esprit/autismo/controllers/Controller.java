
package com.esprit.autismo.controllers;

/*
import javafx.fxml.FXML;
import javafx.scene.SubScene;
import javafx.scene.shape.MeshView;
import javafx.scene.shape.TriangleMesh;
import org.opencv.core.MatOfPoint3f;

public class Controller {

    @FXML
    private SubScene subScene;

    public void initialize() {
        initialize3DContent();
    }

    public void initialize3DContent() {
        // Load the image and convert it to a 3D point cloud
        MatOfPoint3f pointCloud = convertImageTo3D("imgs/valve.png");

        // Create a JavaFX 3D mesh from the point cloud
        TriangleMesh mesh = createMeshFromPointCloud(pointCloud);

        // Create a 3D mesh view
        MeshView meshView = new MeshView(mesh);

        // Add the mesh view to the SubScene
        subScene.setRoot(meshView.getParent());
    }

    private MatOfPoint3f convertImageTo3D(String imagePath) {
        // Implement image-to-3D conversion here
        // This method should return the 3D point cloud
        return null; // Placeholder
    }

    private TriangleMesh createMeshFromPointCloud(MatOfPoint3f pointCloud) {
        // Implement mesh creation here
        // This method should return a JavaFX TriangleMesh
        return null; // Placeholder
    }
}
*/